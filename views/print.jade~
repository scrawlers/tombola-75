extends /views/layout/layout
block content
  .container-fluid
    .row-fluid
      .span12
        .span7
          .summary
            table.table.table-striped.table-bordered.table-hover.table-condensed
              thead
                tr
                  th Category
                  th Production No.
                  th Expiration Date
                  th Quantity
                  th Series
                  th Status
                  th Print Date
              tbody
                - var items = production
                each item in items
                  tr
                    td=item.category
                    td=item.prod_id
                    td=item.expiration
                    td=item.quantity
                    td=item.series
                    td=item.status
                    td=item.prod_date
        .span5
          .summary
            form.form-horizontal(method="POST",action="/print")
              .title-bar
                span.title Generate Batch Game Card
              .control-group
                label.control-label Category
                .controls
                  select.dropdown-toggle.input-medium(id="category", name="category", required="required")
                    option(value="60") Game of 60
                    option(value="75") Game of 75
              .control-group
                label.control-label Quantity
                .controls
                  input.input-medium(type="text", id="quantity", name="quantity", required="required")
              .control-group
                label.control-label Expiration Date
                .controls
                  input.input-medium(type="text", id="expiration", name="expiration", required="required")
              .control-group
                .controls
                  button.btn.btn-success.input-medium(type="submit") Submit
            
block page-scripts
  script(src="/thirdparty/bootstrap/js/jquery.dataTables.min.js",type="text/javascript")
  script(src="/javascripts/print.js")
